# Copy this file to .env and replace every placeholder. .env is gitignored.
# Use a dedicated read-only database role even though the reference PostgreSQL
# MCP server wraps tool queries in read-only transactions.
DATABASE_URL=postgresql://bahnopticon_mcp:change-me@127.0.0.1:5432/bahnopticon
